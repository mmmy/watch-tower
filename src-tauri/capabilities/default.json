{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Signal Desk desktop windows",
  "windows": ["main", "widget"],
  "permissions": [
    "core:default",
    "core:menu:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
