{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main dashboard window",
  "windows": ["main-dashboard"],
  "permissions": ["core:default"]
}
