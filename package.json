{
  "name": "watch-tower-native",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run slint",
    "slint": "node .\\scripts\\run-native.cjs",
    "check": "cargo check --manifest-path native-shell/Cargo.toml",
    "build": "cargo build --manifest-path native-shell/Cargo.toml --release",
    "test": "node .\\scripts\\test-native.cjs"
  }
}
